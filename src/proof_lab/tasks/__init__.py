"""Installed implementations for explicitly registered Proof Lab tasks.

This package intentionally does not import task implementations. The task registry is metadata
only, and the runner imports a builder only after the task crosses the formal admission boundary.
"""
